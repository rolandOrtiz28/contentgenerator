<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generated Social Media Post | Eddie AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="script.js"></script>
    <style>
        :root {
            --main-color: #101010;
            --accent-color: #ff007f;
            --light-gray: #f9f9f9;
        }

        body {
            background: var(--light-gray);
        }

        .container {
            max-width: 700px;
            background: #fff;
            padding: 1.2rem 1.5rem; /* Reduced padding */
            border-radius: 12px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        .label {
            font-weight: 700;
            font-size: 1rem;
            color: var(--main-color);
            display: flex;
            align-items: center;
            gap: 6px;
            margin-bottom: 3px; /* Reduced margin */
        }

        .content-box {
            background: #f4f4f4;
            padding: 0.8rem; /* Reduced padding */
            border-radius: 6px;
            font-size: 0.9rem; /* Slightly smaller font */
            text-align: left;
            white-space: pre-wrap;
            overflow-wrap: break-word;
            color: var(--main-color);
            line-height: 1.3; /* Tightened line spacing */
        }

        .btn {
            display: block;
            margin-top: 0.8rem; /* Reduced margin */
            background: var(--accent-color);
            color: #fff;
            padding: 8px 16px; /* Smaller padding */
            border-radius: 6px;
            font-size: 1rem;
            font-weight: bold;
            transition: 0.3s;
            text-align: center;
        }

        .btn:hover {
            background: #e60073;
        }

        .section {
            margin-bottom: 0.5rem; /* Reduced spacing between sections */
        }

        .icon {
            font-size: 1rem; /* Adjusted size */
            color: var(--accent-color);
        }

        ul {
            padding-left: 0.8rem; /* Less indent */
            margin-top: 2px;
        }

        li {
            margin-bottom: 2px;
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen">
    <div class="container">
        <h1 class="text-lg font-bold text-[var(--main-color)] text-center mb-3">AI-Generated Social Media Post</h1>

        <!-- Generated Content -->
        <div class="content-box">
            <div class="section">
                <span class="label"><span class="icon">📌</span>Topic:</span> 
                <%= content.match(/- \*\*Topic:\*\* (.*)/)?.[1] || "N/A" %>
            

            
                <span class="label"><span class="icon">✍️</span>Caption:</span> 
                <%= content.match(/- \*\*Caption:\*\* (.*)/)?.[1] || "N/A" %>
            

            
                <span class="label"><span class="icon">🔗</span>Hashtags:</span> 
                <%= content.match(/- \*\*Hashtags:\*\* (.*)/)?.[1] || "N/A" %>
            

            
                <span class="label"><span class="icon">📝</span>Visual Text:</span>
                <ul>
                    <li><strong>🔹 Heading:</strong> <%= content.match(/- \*\*Heading:\*\* (.*)/)?.[1] || "N/A" %></li>
                    <li><strong>🔹 Subheading:</strong> <%= content.match(/- \*\*Subheading:\*\* (.*)/)?.[1] || "N/A" %></li>
                    <li><strong>🔹 Bullet Points:</strong></li>
                    <ul>
                        <li>✔ <%= content.match(/- \*\*Bullet Points:\*\* - (.*)/)?.[1] || "N/A" %></li>
                    </ul>
                </ul>
            

           
                <span class="label"><span class="icon">🎨</span>Suggested Assets:</span> 
                <%= content.match(/- \*\*Suggested Assets:\*\* (.*)/)?.[1] || "N/A" %>
          

            
                <span class="label"><span class="icon">📣</span>CTA:</span> 
                <%= content.match(/- \*\*CTA:\*\* (.*)/)?.[1] || "N/A" %>
            </div>
        </div>

        <!-- Generate Another Post -->
        <a href="/" class="btn">Generate Another</a>
    </div>
</body>
</html>
