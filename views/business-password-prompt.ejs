<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Enter Password | Eddie AI</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen bg-gray-100">
  <div class="bg-white shadow-xl rounded-2xl p-8 w-full max-w-lg">
    <h1 class="text-3xl font-bold text-center text-[#101010] mb-2">Enter Business Password</h1>
    <p class="text-gray-600 text-center mb-6">Please enter the password for the selected business to proceed.</p>

    <% if (error) { %>
      <p class="text-red-500 text-center mb-4"><%= error %></p>
    <% } %>

    <form action="/social-media/branding-social-details" method="POST" class="space-y-4">
      <input type="hidden" name="selectedBusiness" value="<%= businessId %>">
      <label class="block text-lg font-semibold">Password:</label>
      <input type="password" name="password" required class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#ff007f] transition">
      <button type="submit" class="w-full bg-[#ff007f] text-white p-3 rounded-lg font-semibold hover:bg-pink-600">Submit</button>
    </form>
  </div>
</body>
</html>